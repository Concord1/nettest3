{
  "name": "nodegyro",
  "version": "1.0.0",
  "description": "May22 version of gyrosend, to be deployed on Netlify",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify index.js -o bundle.js"
  },
  "author": "HK",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^17.0.0"
  },
  "dependencies": {
    "redis": "^4.6.6"
  }
}
